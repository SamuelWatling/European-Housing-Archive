# R session junk
.Rhistory
.RData
.Rproj.user/

# Regenerable -- the scripts that make it are what matter
output/

# LaTeX build files -- the .tex and its compiled .pdf are tracked, the rest is not
docs/*.aux
docs/*.fdb_latexmk
docs/*.fls
docs/*.log
docs/*.out
docs/*.synctex.gz
